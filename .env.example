# Database
DATABASE_URL=postgresql://reverse_hh:reverse_hh@localhost:5432/reverse_hh

# Auth
JWT_SECRET=change-me-in-production
JWT_EXPIRE_HOURS=24
COOKIE_NAME=access_token

# App
DEBUG=true
FRONTEND_DIR=

# Email (optional in dev — logs to console if empty)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
